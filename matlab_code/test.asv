% ====================================================
% author: Yi Hu (yhu28@ncsu.edu)
% date: 2/6/2024
% evaluate the estimation accuracy for ECE753 homework
% ====================================================

clear all
close all

GT = csvread("../gt.csv"); % dim:80*1, real data, will be available after due date
M = csvread("output.csv"); % dim:80*1, your result

mpe = mean(abs(GT-M)./GT);

for i = 1:5
    gt_fft = fft(GT(i*16-15:i*16));
    plot(gt_fft);
    m_fft = fft(M(i*16-15:i*16));
    plot(gt_fft);
%     ffte = mean(abs(gt_fft-m_fft)./gt_fft);
end
% mpe < 3%          10/10
% 3% <= mpe < 5%    9/10
% 5% <= mpe < 10%   8/10
% 10% <= mpe < 20%  7/10
% 20% <= mpe        6/10